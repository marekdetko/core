module "Fauxtoshop"

args =
  submitData: false
  checkForUpdates: false
  devMode: true
testFS = new Fauxtoshop(args)

test "Can get OS", ->
  expect 1
  ok testFS.getOS()

test "Can get data", ->
  expect 1
  equal $.type(testFS.getData()), "object"

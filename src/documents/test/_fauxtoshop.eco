module "Fauxtoshop"

args =
  submitData: false
  checkForUpdates: false
  devMode: true
window.fauxtoshop = new Fauxtoshop(args)

test "Can get OS", ->
  expect 1
  ok fauxtoshop.getOS()

test "Can get data", ->
  expect 1
  equal $.type(fauxtoshop.getData()), "object"

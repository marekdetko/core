module "Fauxtoshop"

test "can get OS", ->
  expect 1
  ok Fauxtoshop.getOS()

test "can get data", ->
  expect 1
  equal $.type(Fauxtoshop.getData()), "object"

test "can toggle guides", ->
  expect 1
  equal Fauxtoshop.toggleGuides(), true

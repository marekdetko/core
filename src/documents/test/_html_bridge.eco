module "HTMLBridge"

testBridge = new GuideGuideHTMLBridge()

test "can get data", ->
  expect 1
  equal $.type(testBridge.getData()), "object"

test "can get document info", ->
  expect 1
  equal $.type(testBridge.getDocumentInfo()), "object"
